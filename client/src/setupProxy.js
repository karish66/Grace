const proxy = require('http-proxy-middleware')
 
module.exports = function(app) {
  // app.use(proxy('/users/login', { target: 'https://wams.herokuapp.com/' }))
  // app.use(proxy('/auth/api/*', { target: 'http://localhost:5000/' }))
  // app.use(proxy('/users/*'),{target:"http://f7fa5261.ngrok.io/"})
  // app.use(proxy('/resume/*',{ target: "http://localhost:5000/"}))
}